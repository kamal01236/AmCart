# Application Gateway (WAF v2) Module

Provisions a WAF v2 Application Gateway with HTTPS listener, Key Vault-backed certificate, and backend pool targeting the AKS ingress controller. Extend routing rules or listeners per environment as needed.
